version: '3'
services:
  app:
    image: 172.21.21.62:8000/dcpms-service-dict-info:docker
    container_name: dcpms-service-dict-info
    restart: always
    volumes:
      - ./bootstrap.yml:/app/bootstrap.yml
      - ./logback.xml:/app/logback.xml
      - ./logs:/app/logs
      - /data/resource:/data/resource
      - /data/temp:/data/temp
    environment:
      - JAVA_OPTS=-Xmx256m
      - SPRING_PROFILE=dev
    network_mode: host
